.hidden
  - for file in Dir.glob("app/views/home/tips/*")
   - file = File.basename(file).scan(/_(\w+)/).to_s
   div[id=file]= render "home/tips/#{file}"



= render 'top'
= render 'menu'
= render 'about'
= render 'speakers'
/ = render 'speaker'
= render 'nilbies'
= render 'tickets'

= render 'sponsors'
= render 'footer'
.clear